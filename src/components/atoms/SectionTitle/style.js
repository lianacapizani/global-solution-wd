import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    justifyContent: 'left',
    gap: 10,
    alignItems: 'center',
    marginTop: 20,
    paddingHorizontal: 10,
    marginBottom: 10,
  },
  title: {
    fontSize: 18,
  },
});
